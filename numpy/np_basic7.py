#!/usr/bin/env python3

import numpy as np

#itemsize sirve para saber el tamaño en memoria de una array y size para saber el tamaño de la array en si

a = np.array([1, 7, 13, 105])

print(a.itemsize)
print(a.size)
